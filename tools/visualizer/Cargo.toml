[package]
name = "intent-visualizer"
version = "0.1.0"
edition = "2021"

[dependencies]
intent-syntax = { path = "../../crates/intent-syntax" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"

[dev-dependencies]
insta = "1.34"
