[package]
name = "intent-core"
version.workspace = true
edition.workspace = true

[dependencies]
intent-syntax = { path = "../intent-syntax" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
z3 = { version = "0.20", features = ["vendored"] }
